import withLayoutBasic from "@/libs/LayoutBasic";
import { Container, Stack } from "@mui/material";

const PropertyDetail = () => {
  return <Container>PROPERTY DETAIL</Container>;
};

export default withLayoutBasic(PropertyDetail);
